#include<stdio.h> 
main()
{
		float side,area;
		
	printf("side is the length of a side of the square : ");
	scanf("%f",&side);
	
	area=side*side;
	
	printf("calculate the Area of a square  = %.2f\n",area);
}